# 双应用业务逻辑/规则审计报告（2026-08-14）

> **范围**：预算应用 Budget（D:\109-BudgetEstimate）、工时应用 TR（D:\108-TimeRecording）、共享后端（backend）。
> **方法**：4 个并行审计 agent 携带业务规则简报分域审计（Budget 分析 / Budget 编制·交付·审批 / 后端 / TR 工时）→ 返回候选问题（文件/行/规则/证据/置信度）→ 逐条对照真实代码复核真实性（不经复核不采信）→ 确认真实且可安全修复的问题：修复 + 回归测试接入编译门禁 → 更新家族缺陷清单。
> **门禁**：Budget `lint-naming + lint + tsc -b + vite build + vitest 189 全绿`；backend `tsc + vitest 64 全绿`。

## 一、确认真实并已修复（7 项，含回归测试）

| 编号 | 位置 | 规则违反 | 修复 |
|---|---|---|---|
| **B1/A1** | `BudgetEstimateApp/src/utils/calculations.ts` L13/15/106、`costBreakdown.ts` L57/66/74/90/91 | 数量可输入 0（`EditableNumberInput min=0`）且 DB `qty_total INTEGER NOT NULL DEFAULT 1` 无 `CHECK>0`，`qtyTotal \|\| 1` 把合法 0 吞成 1 → 成本按 ×1 虚增、物料/人工分解错归 | 全部 `\|\| 1` → `?? 1`（缺省仍按 1，显式 0 保留为 0） |
| **A2** | `BudgetEstimateApp/src/pages/Dashboard.tsx` L402 | Dashboard 节点按时率内联 `node.actualDate \|\| delay.delayed`，把无审批基线（baselinePlannedEndDate 缺失）的已完成节点计为「按时」→ 未审批实施计划的项目节点按时率恒 100%；DeliveryAnalysis「无基线不判定」（refEnd 缺失 continue），两页口径打架 | 提取 `computeProjectOnTimeRate` 共享纯函数，`delay.hasBaseline && (actualDate \|\| delayed)` 剔除无基线节点；Dashboard 改用 |
| **B5** | `BudgetEstimateApp/src/pages/DeliveryDetail.tsx` L277 | 节点15（项目总结）切「已完成」仅校验成本审批；成本通过后节点3 仍 pending 即可提前完结 → 分析以节点15完成日做交付/订单/销售归集，把未完成项目计为已完结 | 提取 `projectCompletionGate` 共享谓词（成本审批 + 其余全部节点 completed），节点15 切换与「完成项目」按钮共用 |
| **C1** | `backend/src/routes/timerecording.ts` L1087 | POST /admin/users 建号路径仅 `requireRole('director','admin')` 无 `assertCanManage` → **director(2) 可铸 admin(3) 号**；同文件 reset-password（L1138）、users.ts 创建（L55）均有防线 | 创建路径补 `assertCanManage(req.user!.role, ROLE_RANK[role] ?? 0)` |
| **C2** | `backend/src/routes/approvals.ts` L52 | POST 创建审批无 approval_type 枚举白名单，非法值直插 PG 枚举列 → PG 枚举错误被 500 吞（泄漏内部错误语义） | 白名单 `[quotation, plan, cost, promote]` → 非法值 400 |
| **C3** | `backend/src/routes/deliveries.ts` nodes PUT L168 | node.history 非数组时 `JSON.stringify` 落库为畸形 JSON，渲染时 `DeliveryNodeTimeline` 的 `.find/.map` 崩溃 | `Array.isArray(node.history)` 校验 → 非法 400 |
| **C4** | `backend/src/routes/deliveries.ts` nodes PUT L168 | node.status 无枚举白名单，非法值触发 PG 枚举错误 → 500 | 白名单 `[pending, in_progress, completed]` → 非法 400 |

**新增测试**：Budget `tests/calculations.test.ts`（qty=0）+1、`tests/analysisShared.test.ts`（computeProjectOnTimeRate ×3 / projectCompletionGate ×4）+7、`tests/businessRulesAudit.test.ts`（静态接线断言 ×5）+5；backend `tests/audit-rules.test.ts`（静态源码断言 ×4）+4。合计 **Budget +13 / backend +4**（另含既有门禁全量回归）。

## 二、复核为「设计意图 / 非缺陷」（4 项，已文档化，不改代码）

| 编号 | 位置 | 审计发现 | 复核结论 |
|---|---|---|---|
| **B2** | SalesOpportunityList 金额列 | 任何报价（含草稿）即锁定金额列 | **REFUTED**——2026-07-14 刻意修复（sales-opportunity-field-lock-pattern）：状态机为「有报价(draft/pending/approved) → 锁定金额」，非「已审批才锁」 |
| **B3** | ProjectHeader clientName/salesNo | 草稿态可编辑客户名/销售编号，与机会侧同名字段只读口径分裂 | 设计意图——机会与报价是独立记录；机会侧锁定源于「创建后不可改归属」，报价草稿允许修正属产品选择。留待产品决策 |
| **B4** | QuotationPage 已审批报价保存降级 draft | 审批通过报价被解锁，改后保存静默重置 draft | 设计意图——B61 刻意防绕过（改过数据不经重审仍显示已通过）；「静默降级无警告」是 UX 缺口，非逻辑错误。留待产品决策 |
| **B7** | project_versions 列持久化含税 | 规则文档称列未税、展示 ×(1+税)；代码存含税、展示不乘（前端自洽） | 契约文档与实现长期分叉；前端自洽、后端消费方不二次计税。改动任一侧都有双重计税风险，违反「不引入新问题」约束。**契约决策点**，不擅改 |

## 三、复核为「需产品决策 / TR 侧」（4 项，未修）

| 编号 | 位置 | 审计发现 | 复核结论 |
|---|---|---|---|
| **D1** | TimeRecord.jsx 周编辑权限 | 唯一门禁是 weekFrozen（已提交周冻结），历史周/未来周可自由编辑增删 | 规则文档「当前周开放；历史周只读」，但产品允许补录（未提交周可改）是有意设计；锁定历史周会阻断补录。**需产品确认**补录是否允许 |
| **D2** | TimeRecord.jsx 调休上班日请休假 | MAKEUP_WORKDAYS（周六补班）计入应出勤分母，但 validateGrid 对 wd=0/6 一律拒请休假 → 补班日请假被拦、开工率分母不减 | 后端 serverHourType/assertLeaveValid 同样按周末拒（注释明确「MAKEUP_WORKDAYS 仅前端使用，后端无需维护」）——补班仅调分母是**既有设计**；支持补班日请休假需前后端同改 + HR 规则确认。**需产品决策** |
| **D3** | TimeRecord.jsx 全天请休假 ≤8h | 上午+下午整段 = 9h > 8h，全天请假被拦（须手动压到 ≤8h） | 规则「单日 ≤8h」与时段系统日净工时（8h vs 时段总和 9h）内部不自洽，常见全天请假被误拦。**需产品决策**上限口径 |
| **D4** | AdminApproval.jsx 时段外记录标识 | slotOf 对 12:30 间隙等时段外起时返回 null → 兜底归 evening 列，但「其他」徽标仅在 start_time 为空时显示 | UI 标识不一致（TimeRecord 页放「其他时段记录」区块，Admin 页原样显示在晚间列）；仅显示差异，无数据/逻辑错误。低危 |

## 四、门禁集成

- 新增测试已通过既有 build 门禁（Budget `npm run build` 末尾 `npx vitest run` 自动执行；backend `tsc && vitest run`），无需改脚本。
- 后端静态源码断言沿用 `audit-fixes.test.ts` / `route-guards.test.ts` 的 readFileSync 模式（无 DB、无 mock）。

## 五、防回归要点（已并入家族清单 F01/F02/F04/F08）

1. 数量类 `|| 1` 兜底 → `?? 1`（F04）——自查新增报价项/成本计算是否仍用 `|| 1` 吞合法 0。
2. 建号/改权接口逐条核对 `assertCanManage`（F02）——**创建**路径最易漏（reset 有、create 漏）。
3. 同一指标多页展示 → 提共享纯函数（F08）——节点按时率/项目完成门禁已收敛，勿再内联口径。
4. 新写 POST/PUT 写 PG 枚举列/JSON 列 → 应用层白名单/数组校验（F01）。
