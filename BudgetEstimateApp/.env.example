# 109-BudgetEstimate 环境变量配置

# API 基础路径（后端服务地址）
# 开发环境使用 Vite 代理（vite.config.ts 中的 proxy 配置），无需修改
# 生产环境需设置为实际后端 URL，如 https://api.example.com/api/v1
VITE_API_BASE=/api/v1

# Vite 开发代理目标地址（默认指向生产服务器 IP）
# 本地开发时可改为 http://localhost:3000
# VITE_PROXY_TARGET=http://localhost:3000
